---
import { getCollection } from "astro:content";
import BaseLayout from "../components/BaseLayout.astro";
import Section from "../components/Section.astro";
import PostCard from "../components/PostCard.astro";

const posts = (
  await getCollection("posts", ({ data }) => !data.draft)
).sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());

const latestPosts = posts.slice(0, 3);

function formatDate(date: Date): string {
  return new Intl.DateTimeFormat("en-US", {
    year: "numeric",
    month: "short",
    day: "numeric",
  }).format(date);
}

function shortSlug(slug: string): string {
  return slug.split("/").pop() || slug;
}
---

<BaseLayout
  title="Patel of Thought"
  description="Long-form engineering notes, pragmatic systems thinking, and video walkthroughs for people who ship."
  image="/favicon_21_9.png"
  navVariant="patel"
>
  <section class="platform-hero">
    <div class="container platform-hero-grid">
      <div class="platform-hero-copy">
        <h1 class="platform-title">Patel of Thoughts
          <br/>
          A tech platform for learners and builders.</h1>
        <p class="platform-tagline">
          Long-form engineering notes, pragmatic systems thinking, and video
          walkthroughs for people who ship.
        </p>
        <!-- <div class="platform-actions">
          <a class="button" href="/blog">Read the blog</a>
          <a
            class="button ghost"
            href={profile.links.youtube}
            target="_blank"
            rel="noreferrer"
          >
            Watch on YouTube
          </a>
        </div> -->
        <div class="platform-metrics">
          <div class="metric">
            <p class="metric-label">Focus</p>
            <p class="metric-value">Engineering, AI, Product</p>
          </div>
          <div class="metric">
            <p class="metric-label">Format</p>
            <p class="metric-value">Essays, playbooks, videos</p>
          </div>
          <div class="metric">
            <p class="metric-label">Cadence</p>
            <p class="metric-value">Bi-weekly drops</p>
          </div>
        </div>
      </div>
      <div class="platform-hero-card">
        <div class="platform-hero-card-inner">
          <p class="card-eyebrow">Latest drop</p>
          <h3>System design notes for modern teams</h3>
          <p class="text-secondary">
            Practical checklists, architecture decisions, and the tradeoffs you
            actually hit in production.
          </p>
          <!-- <div class="platform-hero-links">
            <a class="text-link" href="/blog">Browse the library</a>
            <a
              class="text-link"
              href={profile.links.youtube}
              target="_blank"
              rel="noreferrer"
            >
              Watch highlights
            </a>
          </div> -->
        </div>
      </div>
    </div>
  </section>

  <div class="container content-stack">
    <Section title="Latest Blog Posts" subtitle="Fresh writing from Patel of Thought.">
      <div class="card-grid">
        {latestPosts.map((post) => (
          <PostCard
            title={post.data.title}
            description={post.data.description}
            href={`/posts/${shortSlug(post.slug)}`}
            cover={post.data.cover}
            date={formatDate(post.data.date)}
            tags={post.data.tags}
          />
        ))}
      </div>
      <div class="section-cta">
        <a class="button ghost" href="/blog">Explore all posts</a>
      </div>
    </Section>

    <!-- <Section
      title="Video Library"
      subtitle="Deep dives, walkthroughs, and short-form engineering lessons."
    >
      <div class="video-grid">
        <article class="video-card">
          <p class="video-label">Architecture</p>
          <h3>Breaking down real-world system designs</h3>
          <p class="text-secondary">
            Case studies from modern platforms, with the tradeoffs called out.
          </p>
          <a
            class="text-link"
            href={profile.links.youtube}
            target="_blank"
            rel="noreferrer"
          >
            Watch the series
          </a>
        </article>
        <article class="video-card">
          <p class="video-label">AI in practice</p>
          <h3>Shipping AI features responsibly</h3>
          <p class="text-secondary">
            Practical guidance on building reliable, observable AI products.
          </p>
          <a
            class="text-link"
            href={profile.links.youtube}
            target="_blank"
            rel="noreferrer"
          >
            Watch the series
          </a>
        </article>
        <article class="video-card">
          <p class="video-label">Career</p>
          <h3>Leadership, mentoring, and growth</h3>
          <p class="text-secondary">
            Clear frameworks for senior engineers and tech leads.
          </p>
          <a
            class="text-link"
            href={profile.links.youtube}
            target="_blank"
            rel="noreferrer"
          >
            Watch the series
          </a>
        </article>
      </div>
      <div class="section-cta">
        <a
          class="button"
          href={profile.links.youtube}
          target="_blank"
          rel="noreferrer"
        >
          Visit the YouTube channel
        </a>
      </div>
    </Section> -->
  </div>
</BaseLayout>

<style>
  .platform-hero {
    padding: var(--space-3xl) 0 var(--space-2xl);
  }

  .platform-hero-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: var(--space-2xl);
    align-items: center;
  }

  .platform-hero-copy {
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
  }

  .platform-title {
    margin: 0;
    font-size: clamp(2.4rem, 4vw, 3.6rem);
  }

  .platform-tagline {
    font-size: var(--font-size-lg);
    color: var(--color-text-secondary);
  }

  .platform-actions {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-md);
  }

  .platform-metrics {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
    gap: var(--space-lg);
    padding-top: var(--space-md);
  }

  .metric {
    background-color: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    padding: var(--space-md);
  }

  .metric-label {
    margin: 0 0 var(--space-xs);
    font-size: var(--font-size-xs);
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: var(--color-text-secondary);
  }

  .metric-value {
    margin: 0;
    font-weight: 600;
  }

  .platform-hero-card {
    background: linear-gradient(140deg, rgba(10, 91, 255, 0.2), rgba(255, 183, 3, 0.18));
    padding: 2px;
    border-radius: var(--radius-lg);
  }

  .platform-hero-card-inner {
    background-color: var(--color-bg-secondary);
    border-radius: calc(var(--radius-lg) - 2px);
    padding: var(--space-xl);
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
    box-shadow: 0 24px 60px rgba(0, 0, 0, 0.08);
  }

  .card-eyebrow {
    margin: 0;
    font-size: var(--font-size-xs);
    text-transform: uppercase;
    letter-spacing: 0.22em;
    color: var(--color-text-secondary);
  }

  .platform-hero-links {
    display: flex;
    gap: var(--space-md);
    flex-wrap: wrap;
  }

  .section-cta {
    display: flex;
    justify-content: center;
    margin-top: var(--space-lg);
  }

  .video-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: var(--space-lg);
  }

  .video-card {
    background-color: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    padding: var(--space-lg);
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
  }

  .video-label {
    margin: 0;
    font-size: var(--font-size-xs);
    text-transform: uppercase;
    letter-spacing: 0.2em;
    color: var(--color-text-secondary);
  }

  @media (max-width: 768px) {
    .platform-hero {
      padding-top: var(--space-2xl);
    }

    .platform-title {
      font-size: clamp(2.2rem, 8vw, 3rem);
    }

    .platform-actions {
      width: 100%;
    }
  }
</style>
